import 'package:flutter/material.dart';

class AllFoodsScreen extends StatelessWidget {
  const AllFoodsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('All Foods');
  }
}
